{
  "name": "room-escape-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "ts-node-dev --respawn --transpile-only index.ts",
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "start:docker": "docker compose up app --build"
  },
  "keywords": [],
  "author": "Tuna0917",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "pino-pretty": "^10.2.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
